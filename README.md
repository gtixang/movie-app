<h1 align="center">
  <img src="movie.svg" alt="Movie App Logo" width="38px" height="38px" style="vertical-align: middle; align="center"" />
  angular-movie-app — Тестовое задание на Angular
</h1>


В этом архиве находится полностью готовое приложение **angular-movie-app** (на **Angular 18**) с каталогом `src/`, полной конфигурацией и файлом `db.json` для запуска mock API через `json-server`.

---

## 🚀 Быстрый старт

1. Распакуйте архив и перейдите в папку проекта:

```bash
cd angular-movie-app
```

2. Установите зависимости:

```bash
npm install
```

3. Запустите одновременно mock-API и Angular-приложение:

```bash
npm run start:all
```

После запуска:

- Приложение будет доступно по адресу: **http://localhost:4200**
- API (json-server) — **http://localhost:3000/movies**

---

## 🧰 Технологии и стек проекта

- **Angular 18**
- **TypeScript**
- **RxJS**
- **Signals** (для реактивного обмена данными между компонентами)
- **NgRx Store / Effects** (управление состоянием и побочными эффектами)
- **SCSS** (модульные стили)
- **json-server** (mock API)
- **concurrently** (параллельный запуск клиента и сервера)
- **angular-cli-ghpages** (деплой на GitHub Pages)

---

## ⚙️ Особенности реализации

- Используется `ChangeDetectionStrategy.OnPush` для повышения производительности.
- Применяется `trackBy` в директивах `*ngFor` / `@for` для оптимального обновления DOM.
- Lazy-loading реализован через `loadComponent` для загрузки страницы фильмов по маршруту.
- Реализована детальная страница фильма.
- Обработка ошибок HTTP-запросов с отображением пользовательского сообщения.
- Используется **Signals** для передачи данных между дочерним и родительским компонентами (например, значения из поля поиска).
- Адаптивная верстка и семантическая структура HTML.

---

## 📂 Структура проекта (основные разделы)

- `src/` — исходный код приложения
  - `app/` — компоненты, состояние (NgRx), эффекты, модели
  - `assets/` — изображения и постеры фильмов
  - `styles.scss` — глобальные стили
- `db.json` — mock-данные для `json-server`
- `package.json`, `angular.json`, `tsconfig*.json` — конфигурационные файлы
- `README.md` — текущая инструкция

---

## 🌐 Сборка проекта

Для сборки выполните:

```bash
npm run build
```

Результат сборки будет находиться в папке `dist/`.


## 💡 Возможное развитие проекта

- Добавление фильтрации и сортировки фильмов.
- Возможность добавления фильмов в «Избранное».
- Пагинация и виртуальный скролл.
- Тестирование компонентов (Jest / Karma).
- Улучшение UI/UX и анимаций с использованием Angular Animations.
